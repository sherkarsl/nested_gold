<%= form_for(@project_category) do |f| %>
  <% if @project_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_category.errors.count, "error") %> prohibited this project_category from being saved:</h2>

      <ul>
      <% @project_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
